---
# When enabled proceed to the deletion of the TLS certificate issued by
# let's encrypt at teardown (mainly helps avoiding the reaching of the quota of
# 5 certificates generated by weeks by domain)
traefik__enable_tls_certificates_deletion: true
# When enabled proceed to the deletion of the main traefik configuration file
traefik__enable_traefik_file_deletion: true
